% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent-gadget.R
\name{start_agent}
\alias{start_agent}
\alias{start_rflowlabs}
\title{Launch the RflowLabs agentic coding assistant in RStudio}
\usage{
start_agent(project_dir = getwd(), model = "sonnet", browser = FALSE)

start_rflowlabs(project_dir = getwd(), model = "sonnet", browser = FALSE)
}
\arguments{
\item{project_dir}{Working directory for file operations (default \code{getwd()}).}

\item{model}{One of "sonnet" (default), "opus", "haiku", or a full model id
(e.g. "claude-fable-5").}

\item{browser}{Open in an external browser instead of the RStudio viewer pane.}
}
\value{
Invisible; called for the side effect of running the gadget.
}
\description{
Opens the RflowLabs Shiny gadget: an agentic coding assistant that runs on your
Claude Code subscription (no API key) and executes the agent's R in your live
RStudio session, so plots render in the Plots pane and objects appear in the
Environment while code and output stream into the gadget. \code{start_rflowlabs()}
is an alias for \code{start_agent()}.
}
\details{
Requires the Claude Code CLI installed and logged in, plus the suggested
packages shiny, processx, and later (install with
\code{install.packages(c("shiny", "processx", "later"))}).
}
\examples{
\dontrun{
start_rflowlabs()
}
}
